# `@vibecook/avocado` (CLI — `avo`)

```sh
npm i -g @vibecook/avocado
avo --help
```

The CLI connects to a host process (e.g. the playground app) via the IPC transport and shares your shell session over the mesh.

## Commands

*The CLI surface is still stabilizing — run `avo --help` for the authoritative list.*
